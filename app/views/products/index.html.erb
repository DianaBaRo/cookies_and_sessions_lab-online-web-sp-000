<%= cart.each do |product| %>
  <%= product.name %>
<% end %>

<%= form_tag("/products") do %>
  <%= label_tag('product[name]', "Name") %>
  <%= text_field_tag('product[name]') %>

  <%= submit_tag "Add item" %>
<% end %>
